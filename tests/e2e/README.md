# Browser acceptance tests

Playwright is installed and configured for Chromium. Add player-journey tests here as the UI is implemented (AC-17). No gameplay tests exist yet; `pnpm test:e2e` reports no tests until then.
