# Synthetic credentials for local development only.
POSTGRES_USER=dinopump
POSTGRES_PASSWORD=dinopump_local_only
POSTGRES_DB=dinopump
POSTGRES_PORT=55432
DATABASE_URL=postgresql://dinopump:dinopump_local_only@127.0.0.1:55432/dinopump
GAME_SERVER_PORT=3001
